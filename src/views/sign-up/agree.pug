extends ../layout

block head 
block body
  div
    h2 어서오세요
      br
      | 약관동의가 필요합니다.
    form
      div 
        input(id="agree-all", type="checkbox")
        label(for="agree-all") 전체동의
      hr
      each agreeItem, index in agreeList
        div
          input(id=agreeItem.id, name=agreeItem.id, type="checkbox", required=agreeItem.required)
          label(for=agreeItem.id)=`${agreeItem.label} ${!agreeItem.required ? "(선택)" : ""}`
      hr